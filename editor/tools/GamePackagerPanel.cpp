#include "GamePackagerPanel.h"

namespace atlas::editor {

void GamePackagerPanel::Draw() {
    // Stub: In a real implementation, this would render via Atlas UI
    // Display build target, mode, output path, options, build button
    // Build flow: Validate → Cook → Strip → Bundle → Copy Runtime → Emit
}

}
